/*jshint asi:true */
// Shared code (browser and server)
define(function () {

  // Save some typing for array item/substring checks
  var checkContains = function(array, item) {
   return ( array.indexOf(item) !== -1)
  };

})  
